# layout2
This project is a responsive layout built using HTML and CSS with Flexbox. The main goal was to practice modern CSS layout techniques by structuring the page using Flexbox instead of traditional positioning methods. The layout consists of multiple sections such as header, content area, and sidebar arranged using flexible containers to ensure proper
